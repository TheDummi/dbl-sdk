{
	"name": "dbl-sdk",
	"version": "2.0.2",
	"description": "An sdk for discord bot lists.",
	"main": "dist/main.js",
	"type": "module",
	"scripts": {
		"build": "npm i && tsc && npx prettier --write src dist"
	},
	"devDependencies": {
		"prettier": "3.0.0",
		"readline": "1.3.0",
		"typescript": "5.3.2"
	}
}
